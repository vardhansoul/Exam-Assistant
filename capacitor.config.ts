import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clubofcompetition.app2026',
  appName: 'Club of Competition',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
